const express = require('express');
const cors = require('cors');
const PORT = process.env.PORT || 5000;


app.use((cors));
app.use((express));

app.listen(PORT, () => {
  //what the server should run on the PORT.
});

